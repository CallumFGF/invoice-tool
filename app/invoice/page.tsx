import { InvoiceBuilder } from '@/components/invoice/invoice-builder';

export const metadata = { title: 'Invoice — Childminder Invoice' };

export default function InvoicePage() {
  return <InvoiceBuilder />;
}
