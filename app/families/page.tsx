import { FamilyList } from '@/components/families/family-list';

export const metadata = { title: 'Families — Childminder Invoice' };

export default function FamiliesPage() {
  return <FamilyList />;
}
